import { Router } from 'express';
//import animalRoutes from './animalRoutes'; Preciso criar as rotas de animal  

const routes = Router();
//routes.use('/animais',animaisRoutes);

export default routes;